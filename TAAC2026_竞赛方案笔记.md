# 腾讯广告算法大赛（TAAC 2026）—— 工作流程 & 项目架构笔记

> 创建时间：2026-04-22  
> 作者：AI工程师辅助设计  
> 赛题类型：广告转化率预估（pCVR Prediction）

---

## 一、赛题理解

### 1.1 核心目标

设计一个**统一的、可堆叠的建模模块（Unified Stackable Block）**，能够在同一架构中**同时处理**：
- **多域非序列特征（Multi-field Non-Sequential Tokens）**：用户属性、广告属性、上下文特征、交叉特征
- **行为序列特征（Sequential Behavior Tokens）**：用户历史交互序列（曝光/点击/转化）

**最终输出**：预测目标广告的转化率（pCVR），评估指标为 **AUC-ROC**。

### 1.2 数据集结构

| 数据类型 | 字段说明 | 特征格式 |
|---------|---------|---------|
| **序列数据（Sequential Data）** | user_id + seq[(item_id, action_type, timestamp), ...] | 时序交互日志 |
| **用户特征（User Features）** | 婚姻状态、性别、年龄等 | int_value / int_array |
| **广告特征（Item/Ads Features）** | 类型、类别、广告主类型等 | int_value |
| **上下文特征（Context Features）** | 设备品牌、操作系统等 | int_value (稀疏) |
| **交叉特征（Cross Features）** | User Embedding 等 | float_array (稠密向量) |

> ⚠️ 所有稀疏特征已匿名化为整数 ID，稠密特征为定长浮点向量，无原始文本/图片/URL。

### 1.3 任务格式

每条训练/测试样本对应一个三元组：**(user, context, target ad/item)**  
标签：`y ∈ {0=No Conv., 1=Conv.}`  
训练目标：最大化 AUC-ROC

---

## 二、项目架构设计

```
TAAC2026/
├── data/                      # 数据目录
│   ├── raw/                   # 原始 JSON 数据
│   ├── processed/             # 预处理后的特征文件
│   └── splits/                # train / val / test 划分
│
├── src/                       # 核心源码
│   ├── data/
│   │   ├── dataset.py         # Dataset 类（序列 + 非序列联合加载）
│   │   ├── tokenizer.py       # Sequential Tokenizer & Non-Seq Tokenizer
│   │   └── feature_eng.py     # 特征工程（统计特征、交叉特征构造）
│   │
│   ├── models/
│   │   ├── unified_block.py   # 核心：统一处理模块 Unified Block
│   │   ├── seq_encoder.py     # 序列编码器（Transformer/LSTM/GRU）
│   │   ├── nonseq_encoder.py  # 非序列编码器（MLP/Embedding Lookup）
│   │   ├── cvr_head.py        # CVR 预测头（sigmoid 输出）
│   │   └── model.py           # 整体模型组装
│   │
│   ├── training/
│   │   ├── trainer.py         # 训练循环 + 梯度更新
│   │   ├── loss.py            # CrossEntropyLoss / FocalLoss
│   │   └── scheduler.py       # 学习率调度
│   │
│   ├── evaluation/
│   │   ├── metrics.py         # AUC-ROC 计算
│   │   └── evaluator.py       # 验证集评估
│   │
│   └── utils/
│       ├── config.py          # 超参数配置（dataclass / yaml）
│       ├── logger.py          # 训练日志
│       └── seed.py            # 随机种子固定
│
├── configs/                   # 实验配置文件
│   ├── baseline.yaml          # 基线模型配置
│   ├── unified_block_v1.yaml  # 统一 Block v1
│   └── unified_block_v2.yaml  # 统一 Block v2（Scaling）
│
├── experiments/               # 实验记录
│   └── exp_{date}_{tag}/      # 每次实验自动归档
│       ├── config.yaml
│       ├── train.log
│       └── checkpoints/
│
├── notebooks/                 # 分析 Notebook
│   ├── 01_EDA.ipynb           # 数据探索
│   ├── 02_feature_analysis.ipynb
│   └── 03_model_debug.ipynb
│
├── scripts/
│   ├── preprocess.py          # 数据预处理脚本
│   ├── train.py               # 训练入口
│   ├── evaluate.py            # 评估入口
│   └── submit.py              # 生成提交文件
│
├── requirements.txt
└── README.md
```

---

## 三、完整工作流程

### Phase 0：环境搭建（第 1 天）

```bash
# 建议环境
Python 3.10+
PyTorch 2.x (CUDA 11.8+)
pip install torch transformers scikit-learn pandas numpy tqdm wandb pyyaml
```

- [ ] 创建虚拟环境
- [ ] 下载并解压官方数据集
- [ ] 跑通 `src/data/dataset.py` 能正确加载一批样本

---

### Phase 1：数据探索与理解（第 2-3 天）

**目标**：深度理解数据分布，为特征工程和模型设计提供依据。

**关键分析点**：
1. **序列长度分布**：用户行为序列平均/最大长度 → 决定序列截断策略
2. **action_type 分布**：各行为类型（曝光=1/点击=2/转化=3/4）的占比
3. **标签分布（正负比）**：pCVR 任务通常极度不平衡（转化率 1%-5%）
4. **特征稀疏性**：int_value 特征的基数（cardinality）统计
5. **Cross Feature 维度**：float_array 的长度和数值分布（均值/方差）

**输出产物**：`notebooks/01_EDA.ipynb` + 数据分布报告

---

### Phase 2：特征工程（第 4-5 天）

**序列特征处理**：
| 处理方式 | 说明 |
|---------|------|
| 时间窗口截断 | 保留最近 N 条（N=50/100/200 消融） |
| action_type 权重 | 转化行为赋予更高权重 |
| 时间衰减 | 距离当前时间越远的行为权重递减 |
| target item 对齐 | 提取与目标广告 item_id 相同的历史行为作为强信号 |

**非序列特征处理**：
| 处理方式 | 说明 |
|---------|------|
| Embedding Lookup | int_value → trainable embedding |
| int_array 特征 | 多值 embedding + 求均值/max pooling |
| float_array（Cross）| 直接接 MLP 投影到统一维度 |
| 特征归一化 | float 类型特征做 z-score 归一化 |

---

### Phase 3：模型设计（第 6-10 天）

#### 3.1 基线模型（Baseline）

```
Input
  ├─ Sequential Features → [Embedding] → [Seq Encoder (Transformer)] → seq_repr
  └─ Non-Seq Features    → [Embedding] → [MLP] → nonseq_repr
         ↓
  [Concat(seq_repr, nonseq_repr)]
         ↓
  [MLP Head] → sigmoid → pCVR
```

**基线选型建议**：DIN / DIEN / SIM（工业界成熟方案，快速跑通验证管道）

---

#### 3.2 核心创新：Unified Block 设计

赛题明确鼓励设计**统一的可堆叠模块**，灵感来源于 Transformer，但要同时处理序列 Token 和非序列 Token。

```
┌──────────────────────────────────────────────────┐
│                   Unified Block                   │
│                                                  │
│  Sequential Tokens   Non-Sequential Tokens        │
│  [t1, t2, ..., tS]   [f1, f2, ..., fNS]          │
│        ↓                      ↓                   │
│  [Self-Attention]      [Self-Attention]            │
│        ↓                      ↓                   │
│  ┌────────────────────────────────┐               │
│  │   Cross-Attention / Fusion      │               │
│  │   (Seq attends to Non-Seq       │               │
│  │    and vice versa)              │               │
│  └────────────────────────────────┘               │
│        ↓                      ↓                   │
│    seq_out             nonseq_out                  │
└──────────────────────────────────────────────────┘
         ↓（堆叠 N 个 Unified Block）
   CVR Prediction Head → sigmoid
```

**关键设计选择**：

| 模块 | 可选方案 | 推荐 |
|------|---------|-----|
| 序列编码 | Transformer / Mamba / GRU | Transformer（可并行） |
| 非序列编码 | MLP / FM / DCN | DCNv2（显式特征交叉） |
| 融合方式 | Concat / Cross-Attention / Gating | Cross-Attention |
| 位置编码 | Learnable / RoPE / 无 | RoPE（适合长序列） |

---

#### 3.3 Scaling 探索方向（加分项）

赛题鼓励探索 **Scaling Laws**，建议消融：
- Block 数量：1 / 2 / 4 / 8
- 隐层维度：64 / 128 / 256 / 512
- 序列长度：50 / 100 / 200
- 参数量 vs AUC 曲线

---

### Phase 4：训练与调优（第 11-16 天）

#### 训练策略
```python
# 关键超参数
batch_size = 1024 ~ 4096    # 工业数据集推荐大 batch
learning_rate = 1e-3        # AdamW
weight_decay = 1e-5
warmup_steps = 1000
max_epochs = 10
gradient_clip = 1.0

# 损失函数
loss = BCEWithLogitsLoss(pos_weight=neg/pos_ratio)  # 处理正负样本不均衡
```

#### 调优清单
- [ ] **学习率调度**：Cosine Annealing with Warmup
- [ ] **正负样本平衡**：pos_weight 或 下采样负样本
- [ ] **Dropout 消融**：0.1 / 0.3 / 0.5
- [ ] **序列长度消融**：影响最大的超参之一
- [ ] **Embedding 维度消融**：32 / 64 / 128
- [ ] **混合精度训练**：`torch.cuda.amp` 加速

---

### Phase 5：集成与提交（第 17-20 天）

#### 模型集成策略
```python
# 方案一：预测值平均（简单有效）
final_pred = (pred_model1 + pred_model2 + pred_model3) / 3

# 方案二：排序后平均（Rank Averaging，更鲁棒）
from scipy.stats import rankdata
final_pred = (rankdata(pred1) + rankdata(pred2) + rankdata(pred3)) / 3

# 方案三：Stacking（用小模型融合多个大模型的预测）
```

#### 提交文件格式
```python
# scripts/submit.py
import pandas as pd
submission = pd.DataFrame({
    'sample_id': test_ids,
    'pcvr': predictions  # float, 转化概率
})
submission.to_csv('submission.csv', index=False)
```

---

## 四、时间规划

| 阶段 | 时间 | 主要任务 | 关键产物 |
|-----|------|---------|---------|
| Phase 0 | Day 1 | 环境搭建 + 数据下载 | 可运行的开发环境 |
| Phase 1 | Day 2-3 | EDA + 数据理解 | EDA 报告 |
| Phase 2 | Day 4-5 | 特征工程 | feature_eng.py |
| Phase 3a | Day 6-7 | Baseline 跑通 | 第一个可提交结果 |
| Phase 3b | Day 8-10 | Unified Block 设计 | 核心模型代码 |
| Phase 4 | Day 11-16 | 训练调优 + 消融实验 | 最优单模型 |
| Phase 5 | Day 17-20 | 集成 + 提交 | 最终提交文件 |

---

## 五、核心技术选型汇总

| 模块 | 技术选型 | 理由 |
|-----|---------|-----|
| 深度学习框架 | PyTorch 2.x | 灵活性强，工业界主流 |
| 序列建模 | Transformer (Multi-Head Attention) | 并行训练快，可堆叠 |
| 非序列特征交叉 | DCNv2 / FM | 工业广告场景经典方案 |
| 优化器 | AdamW | 带 weight decay，稳定 |
| 实验追踪 | W&B / MLflow | 实验对比可视化 |
| 数据加载 | PyTorch DataLoader + num_workers | 异步加载，避免 IO 瓶颈 |
| 分布式训练（可选）| DDP (DistributedDataParallel) | 多卡加速 |

---

## 六、风险与注意事项

| 风险点 | 说明 | 应对策略 |
|-------|------|---------|
| 数据格式变动 | 题目说明正式数据可能与示例格式不同 | 数据加载模块做健壮性处理 |
| 序列过长 OOM | 用户行为序列可能很长 | 设置 max_seq_len + 截断策略 |
| 极度类别不平衡 | pCVR 场景转化率极低 | pos_weight / focal loss |
| 特征 ID 分布外 | 测试集出现训练集未见 ID | Embedding 中加入 UNK token |
| 过拟合风险 | 匿名 ID 导致模型记忆训练集 | Dropout + 早停 + 正则化 |

---

## 七、参考论文（建议阅读）

1. **DIN** - Deep Interest Network for Click-Through Rate Prediction (KDD 2018)
2. **DIEN** - Deep Interest Evolution Network (AAAI 2019)
3. **SIM** - Search-based Interest Model (CIKM 2020)
4. **DCNv2** - Improved Deep & Cross Network (WWW 2021)
5. **BST** - Behavior Sequence Transformer for E-commerce Recommendation (2019)
6. **HSTU** - Actions Speak Louder than Words: Trillion-Parameter Sequential Transducers (Meta, 2024)
7. **Transformer** - Attention is All You Need (NeurIPS 2017)

---

*笔记持续更新中，随实验进展补充踩坑经验和调优记录。*
